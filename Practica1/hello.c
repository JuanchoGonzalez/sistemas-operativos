#include <stdio.h>

int main (int argc, char *argv[]) {
    printf("Primer argumento por línea de comandos: %s\n", argv[1]);
    printf("Segundo argumento por línea de comandos: %s\n", argv[2]);
    return 0;
}